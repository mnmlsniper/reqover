<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Bootstrap CSS -->
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous" /> -->
    <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <title>Swagger coverage</title>
    <style>
        .api-path {
            text-align: left;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-dark bg-dark">
        <div class="container">
            <span class="navbar-brand mb-0 h1">Swagger coverage report</span>
        </div>
    </nav>
    <div class="container mt-3">
        <div class="row">
            <div class="col">
                <ul class="nav nav-tabs" id="myTab" role="tablist">
                    <li class="nav-item" role="presentation">
                      <button class="nav-link active" id="all-tab" data-bs-toggle="tab" data-bs-target="#all" type="button" role="tab" aria-controls="all" aria-selected="true">
                          All 
                          <span class="badge bg-secondary rounded-pill"><%= data.all.length %></span>
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                      <button class="nav-link" id="missing-tab" data-bs-toggle="tab" data-bs-target="#missing" type="button" role="tab" aria-controls="missing" aria-selected="false">  
                        Missing
                        <span class="badge bg-danger rounded-pill"><%= data.missing.length %></span>
                    </button>
                    </li>
                    <li class="nav-item" role="presentation">
                      <button class="nav-link" id="partial-tab" data-bs-toggle="tab" data-bs-target="#partial" type="button" role="tab" aria-controls="partial" aria-selected="false">
                          Partial
                          <span class="badge bg-warning rounded-pill"><%= data.partial.length %></span>
                      </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="covered-tab" data-bs-toggle="tab" data-bs-target="#covered" type="button" role="tab" aria-controls="covered" aria-selected="false">
                            Covered
                            <span class="badge bg-success rounded-pill"><%= data.full.length %></span>
                        </button>
                      </li>
                  </ul>
                  <div class="tab-content" id="myTabContent">
                    <div class="tab-pane fade show active" id="all" role="tabpanel" aria-labelledby="all-tab">
                        <%- include('tab', { apiList: data.all });%>
                    </div>
                    <div class="tab-pane fade show active" id="missing" role="tabpanel" aria-labelledby="missing-tab">
                        <%- include('tab', { apiList: data.missing });%>
                    </div>
                    <div class="tab-pane fade show active" id="partial" role="tabpanel" aria-labelledby="partial-tab">
                        <%- include('tab', { apiList: data.partial });%>
                    </div>
                    <div class="tab-pane fade show active" id="covered" role="tabpanel" aria-labelledby="covered-tab">
                        <%- include('tab', { apiList: data.full });%>
                    </div>
                  </div>
            </div>
        </div>
    </div>
    <script src="/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>